<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-4Q6Gf2aSP4eDXB8Miphtr37CMZZQ5oXLH2yaXMJ2w8e2ZtHTl7GptT4jmndRuHDT" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-j1CDi7MgGQ12Z7Qab0qlWQ/Qqz24Gc6BM0thvEMVjHnfYGF0rmFCozFSxQBxwHKO"
        crossorigin="anonymous"></script>
    <style>
        body,
        html {
            height: 100vh;
            margin: 0;
            padding: 0;
            font-family: 'Poppins', sans-serif;
            box-sizing: border-box;
            font-size: 10px;
        }

        .main-width-fluid {
            width: 100%;
            height: 1100px;
            padding-top: 0.5rem;
            display: flex;
            flex-direction: column;
        }

        .gradient-bg {
            background: linear-gradient(120deg, #ffffff 0%, #f5f6f7 40%, #e0e1e2 100%);
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
        }

        .bg-dark-gray {
            background-color: #e8e8e8;
        }

        .section-title {
            font-weight: bold;
            padding: 4px 0;
            margin-bottom: 8px;
        }

        .col-6 {
            width: 49% !important;
        }

        .note-text {
            font-size: 0.8rem;
            color: gray;
        }

        .table th,
        .table td {
            vertical-align: middle;
            text-align: center;
        }
    </style>

</head>

<body>
    <div class="main-width-fluid details-page my-4 py-2 px-3 ">
        <div class="text-end gradient-bg border-bottom py-4 px-2">
            <img src="https://sha-properties.s3.eu-north-1.amazonaws.com/logos/sha-2.png"
                alt="SHARESIDENCES Emirates Logo" height="40">
        </div>
        <div class="d-flex justify-content-center mt-3">
            <div class="text-center bg-dark-gray rounded-pill px-5 sales-container py-2">
                <h4 class="fw-bold mb-0 text-uppercase">Unit Details</h4>
                <p class="mb-0">{{unit_code}}</p>
            </div>
        </div>


        <div class="d-flex justify-content-end mb-2 ">
            {{!-- <!-- <span>Property Reference No: ____________</span> --> --}}
            <span><strong>Date:</strong>{{currentDate}} </span>
        </div>

        <div class="bordered-box mb-2">
            <div class="section-title">PROJECTS DETAILS</div>
            <div class="row justify-content-between  mb-2">
                <div class="col-6 project-name bg-dark-gray hstack justify-content-between">Project Name: <p
                        class="text-end mb-0">SHA Residences</p>
                </div>
                <div class="col-6 bg-dark-gray hstack justify-content-between">Location: <p class="text-end mb-0">SHA
                        Islands AlJurf</p>
                </div>
            </div>
            <div class="row justify-content-between mb-2">
                <div class="col-6 hstack justify-content-between">Project Status: <p class="text-end mb-0">Off-Plan
                    </p>
                </div>
                <div class="col-6 hstack justify-content-between">Estimated Completion Date: <p
                        class="text-end mb-0">Q1 - 2027</p>
                </div>
            </div>

            <div class="section-title">PROPERTY DETAILS & SIZING DETAILS</div>
            <div class="row justify-content-between mb-2 ">
                <div class="col-6 hstack justify-content-between bg-dark-gray">Plot Number: <p class="text-end mb-0">
                        {{unit_code}}</p>
                </div>
                <div class="col-6 hstack justify-content-between bg-dark-gray">Balcony Size: <p
                        class="text-end mb-0">{{bal_sqft}}</p>
                </div>
            </div>
            <div class="row justify-content-between mb-2 ">
                <div class="col-6 hstack justify-content-between">Apartment Type: <p class="text-end mb-0">
                        {{propertyType}} </p>
                </div>
                <div class="col-6 hstack justify-content-between">Total Area: <p class="text-end mb-0">{{sqft}}</p>
                </div>
            </div>


            <div class="row justify-content-between mb-2 ">
                <div class="col-6 hstack justify-content-between bg-dark-gray">Bedrooms: <p class="text-end mb-0">
                        {{bedrooms}}</p>
                </div>
                <div class="col-6 hstack justify-content-between bg-dark-gray">View: <p
                        class="text-end text-capitalize mb-0">{{view}}</p>
                </div>
            </div>

        </div>

        <div class="bordered-box mb-4">
            <div class="section-title">PAYMENT PLAN - 50/50</div>
            <div class="d-flex justify-content-between gap-1 align-item-center">
                <span class="bg-dark-gray fw-bold flex-grow-1 p-2"><strong>Selling Price:</strong>
                    {{full_payment_one}}</span>
                <span class="bg-dark-gray fw-bold flex-grow-1 p-2"><strong>Admin Fee:</strong>AED 2,000</span>
                <span class="bg-dark-gray fw-bold flex-grow-1 p-2">
                    <strong>ADM fee 2%:</strong> {{full_payment_table}}
                    <p class="mb-0 hint-text fw-lighter">*To be paid with booking fee</p>
                </span>
            </div>

            <table class="table table-bordered border-black mt-2">
                <thead>
                    <tr>
                        <th>Description</th>
                        <th>Date</th>
                        <th>Percentage</th>
                        <th>Amount (AED)</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each option1}}
                    <tr>
                        <td>{{label}}</td>
                        <td>{{date }}</td>
                        <td>{{percentage}}</td>
                        <td>{{amount}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>

        <div class="bordered-box mb-4">
            <div class="section-title">PAYMENT PLAN - 70/30 PH</div>
            <div class="d-flex justify-content-between gap-1 align-item-center">
                <span class="bg-dark-gray fw-bold flex-grow-1 p-2"><strong>Selling Price:</strong>
                    {{year_payment_one}}</span>
                <span class="bg-dark-gray fw-bold flex-grow-1 p-2"><strong>Admin Fee:</strong> AED 2,000</span>
                <span class="bg-dark-gray fw-bold flex-grow-1 p-2">
                    <strong>ADM fee 2%:</strong> {{year_payment_table}}
                    <p class="mb-0 hint-text fw-lighter">*To be paid with booking fee</p>
                </span>
            </div>

            <table class="table table-bordered border-black mt-2">
                <thead>
                    <tr>
                        <th>Description</th>
                        <th>Date</th>
                        <th>Percentage</th>
                        <th>Amount (AED)</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each option2}}
                    <tr>
                        <td>{{label}}</td>
                        <td>{{date }}</td>
                        <td>{{percentage}}</td>
                        <td>{{amount}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>

        <p class="note-text fst-italic text-center">
            Note: Please note this payment plan is provided for your information only and is not an obligation or
            commitment by Imkan.
            We calculate the Unit sizes as accurately as possible but they may be subject to final changes. This offer
            remains valid for 24hrs only and is subject to the availability of the Unit. The currency exchange rate will
            be determined at the time of payment.
        </p>
    </div>
</body>

</html>